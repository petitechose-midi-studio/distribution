{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://petitechose-midi-studio.github.io/distribution/schemas/manifest.schema.json",
  "title": "MIDI Studio manifest v2 (outputs)",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema", "channel", "tag", "published_at", "repos", "assets", "install_sets"],
  "properties": {
    "schema": {
      "type": "integer",
      "const": 2
    },
    "channel": {
      "type": "string",
      "enum": ["stable", "beta", "nightly"]
    },
    "tag": {
      "type": "string",
      "minLength": 1
    },
    "published_at": {
      "type": "string",
      "format": "date-time"
    },
    "repos": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["id", "url", "sha"],
        "properties": {
          "id": { "type": "string", "minLength": 1 },
          "url": { "type": "string", "format": "uri" },
          "sha": { "type": "string", "pattern": "^[0-9a-f]{40}$" }
        }
      }
    },
    "assets": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["id", "kind", "filename", "size", "sha256"],
        "properties": {
          "id": { "type": "string", "minLength": 1 },
          "kind": {
            "type": "string",
            "enum": ["bundle", "firmware", "bitwig-extension"]
          },
          "os": { "type": "string", "enum": ["windows", "macos", "linux"] },
          "arch": { "type": "string", "enum": ["x86_64", "arm64"] },
          "filename": { "type": "string", "minLength": 1 },
          "size": { "type": "integer", "minimum": 0 },
          "sha256": { "type": "string", "pattern": "^[0-9a-f]{64}$" },
          "url": { "type": "string", "format": "uri" }
        }
      }
    },
    "install_sets": {
      "type": "array",
      "minItems": 1,
      "contains": {
        "type": "object",
        "properties": {
          "id": { "const": "default" }
        },
        "required": ["id"]
      },
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["id", "assets"],
        "properties": {
          "id": { "type": "string", "minLength": 1 },
          "os": { "type": "string", "enum": ["windows", "macos", "linux"] },
          "arch": { "type": "string", "enum": ["x86_64", "arm64"] },
          "assets": {
            "type": "array",
            "minItems": 1,
            "items": { "type": "string", "minLength": 1 }
          }
        }
      }
    },
    "pages": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "demo_url": { "type": "string", "format": "uri" }
      }
    }
  }
}
